/* Titik masuk ikon. Impor dari sini, bukan dari daftar.ts, supaya kalau
   suatu saat ada ikon buatan sendiri (yang bukan dari Figma) tempatnya sudah
   jelas: di sini, bukan di berkas hasil generate. */
export * from "./daftar";
